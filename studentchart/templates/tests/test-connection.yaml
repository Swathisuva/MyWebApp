apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "studentchart.fullname" . }}-test-connection"
  labels:
    {{- include "studentchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "studentchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
